# Supabase 履歴同期（任意）。未設定でもアプリはローカル(IndexedDB)保存で動く。
# 値を入れて web/.env として保存する（.env は .gitignore 済み）。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 全端末で同じ値にすると履歴が合算される（単一ユーザー前提）。任意の固定文字列でよい。
VITE_USER_ID=me
