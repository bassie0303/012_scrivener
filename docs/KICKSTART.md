# Claude Code キックスタート

新ディレクトリ `012_scrivener` で `claude` を起動したら、まず以下を貼る。

---

```
このプロジェクトは行政書士試験の過去問を解く自分専用アプリ「scrivener」です。
まず CLAUDE.md を読んで、特に「絶対に守ること (IMPORTANT)」の著作権上の制約
（問題本文をオンラインに置かない／同期は履歴だけ／配布しない／欠番1・58〜60の扱い）
を把握してください。

現状:
- data/pipeline/ に問題JSON化(parse_gyosei.py)と正解付与(add_answers.py)の2本がある
- web/GyoseiQuiz.jsx に出題UIの骨組み（3型対応・5択→○×変換・累積履歴）がある。履歴は今メモリ保持。

今日やりたいこと:
（↓ その日の作業を1つ書く。例）
- 令和7年度のフルPDFを実際に処理して data/processed/r7_full.json を生成し、
  欠番が[1,58,59,60]だけか・多肢の語群が20個か・記述の字数が一致するかを検証して。
```

---

## おすすめの初手（CLAUDE.md の TODO 順）

1. `r7_full.json` をフルPDFで生成して精度確認 ← まずここ
2. R2〜R6 も生成して全年度そろえる
3. Supabase接続（`recordHistory()` に upsert を実装し、履歴を永続化・複数端末同期）
4. 出題順ロジック（ランダム / 分野別 / 正答率の低い順 / 間違い再出題）
5. PWA化（manifest + Service Worker）

## セッション運用メモ（既知のやり方）

- タスクは小さく分割。CLAUDE.md を横断記憶として使う。
- コンテキスト60%目安で `/compact`。
- 大きめの作業に入る前に `git` でブランチを切る。
- 新しい仕様・禁止事項が決まったら、`#` 始まりで CLAUDE.md に追記する。
